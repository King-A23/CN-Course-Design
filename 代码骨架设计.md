# DNS Relay 代码骨架设计

## 1. 工程结构

```text
/CMakeLists.txt
/include/dnsrelay/
  config.h
  platform.h
  logger.h
  dns_packet.h
  local_table.h
  pending_map.h
  cache.h
  server.h
/src/
  main.c
  config.c
  platform_win.c
  platform_posix.c
  logger.c
  dns_packet.c
  local_table.c
  pending_map.c
  cache.c
  server.c
/tests/
  test_local_table.c
  test_dns_packet.c
  test_pending_map.c
```

## 2. 模块职责

### 2.1 `config`

负责解析命令行参数：

```text
dnsrelay [-d | -dd] [dns-server-ipaddr] [filename]
```

默认值：

1. 上游 DNS：`202.106.0.20`
2. 本地表文件：`dnsrelay.txt`
3. 监听地址：`0.0.0.0`
4. 监听端口：`53`
5. Cache 容量：`128`
6. 上游超时：`5000ms`

### 2.2 `platform`

屏蔽平台差异，只向核心代码暴露以下能力：

1. socket 初始化与清理。
2. 获取毫秒时间。
3. 关闭 socket。
4. 地址比较、格式化和 IPv4 文本解析。

### 2.3 `logger`

实现三档日志：

1. `DR_LOG_NONE`：无调试信息。
2. `DR_LOG_BASIC`：输出查询来源、ID、域名、类型。
3. `DR_LOG_VERBOSE`：输出本地命中、Cache 命中、转发、超时和迟到包丢弃。

### 2.4 `dns_packet`

负责协议层处理：

1. 解析 DNS Header。
2. 解析 Query 的 `QNAME / QTYPE / QCLASS`。
3. 构造本地 A 记录响应。
4. 构造 `FORMERR / NOTIMP / NXDOMAIN` 错误响应。
5. 从上游响应中提取所有 TTL 偏移。
6. 在 Cache 命中时按已过去秒数重写 TTL。

### 2.5 `local_table`

负责本地静态表：

1. 读取 `dnsrelay.txt`。
2. 域名统一转小写并去掉末尾点。
3. 使用哈希查找本地记录。
4. 重复键按“最后一条覆盖前一条”处理。
5. IP 为 `0.0.0.0` 时视为拦截记录。

### 2.6 `pending_map`

负责上游请求状态：

1. 用 16 位上游 ID 作为索引。
2. 存储客户端 ID、客户端地址、发起时间、查询名、类型、类。
3. 响应到来时按上游 ID 找回原始客户端请求。
4. 超时后删除映射。
5. 迟到应答若查不到映射，直接丢弃。

### 2.7 `cache`

负责缓存上游响应：

1. 缓存键：`qname + qtype + qclass`。
2. 缓存内容：完整响应报文 + TTL 偏移列表。
3. 记录写入时间和过期时间。
4. 命中时重写 Header ID，并把 TTL 改成剩余寿命。
5. 容量满时淘汰最久未使用项。

### 2.8 `server`

负责总控逻辑：

1. 加载本地表。
2. 初始化 socket、上游地址、状态表和缓存。
3. 使用一个 UDP socket 和 `select` 等待数据。
4. 根据“来源地址 + QR 位”区分客户端查询和上游响应。
5. 按“本地命中 -> Cache 命中 -> 上游转发”的顺序处理。
6. 周期性执行超时回收和缓存过期清理。

## 3. 核心数据结构

### 3.1 `DrConfig`

```c
typedef struct DrConfig {
    DrDebugLevel debug_level;
    char upstream_ip[16];
    char table_path[512];
    char bind_ip[16];
    uint16_t bind_port;
    size_t cache_capacity;
    uint32_t upstream_timeout_ms;
} DrConfig;
```

### 3.2 `DrParsedQuery`

```c
typedef struct DrParsedQuery {
    uint16_t id;
    uint16_t flags;
    uint16_t qtype;
    uint16_t qclass;
    uint8_t opcode;
    uint8_t qr;
    char qname[256];
    size_t question_end_offset;
} DrParsedQuery;
```

### 3.3 `DrPendingRequest`

```c
typedef struct DrPendingRequest {
    int active;
    uint16_t upstream_id;
    uint16_t client_id;
    struct sockaddr_storage client_addr;
    socklen_t client_addr_len;
    uint64_t started_ms;
    char qname[256];
    uint16_t qtype;
    uint16_t qclass;
} DrPendingRequest;
```

### 3.4 `DrCacheEntry`

```c
typedef struct DrCacheEntry {
    int in_use;
    char qname[256];
    uint16_t qtype;
    uint16_t qclass;
    uint8_t packet[512];
    size_t packet_len;
    DrTtlPatchList ttl_patches;
    uint64_t stored_at_ms;
    uint64_t expire_at_ms;
    uint64_t last_used_tick;
} DrCacheEntry;
```

## 4. 主处理流程

### 4.1 客户端查询流程

1. `recvfrom()` 收到 UDP 报文。
2. 若 `QR=1` 且来源为上游 DNS，则进入上游响应流程。
3. 否则按客户端查询处理。
4. 校验 Header 长度、`QDCOUNT==1`、`OPCODE==0`。
5. 解析 `QNAME / QTYPE / QCLASS`。
6. 若 `QTYPE=A && QCLASS=IN`：
   - 本地普通命中：构造 A 响应并返回。
   - 本地 `0.0.0.0`：构造 `NXDOMAIN` 并返回。
7. 若本地未命中：
   - 查 Cache，命中则直接返回。
   - 未命中则分配新的上游 ID，记录挂起状态并转发给上游。

### 4.2 上游响应流程

1. 根据响应 ID 在 `pending_map` 中查找挂起请求。
2. 查不到则说明是迟到包或未知包，直接丢弃。
3. 查到后根据条件决定是否写入 Cache。
4. 把响应 ID 改回原客户端 ID。
5. 原样回发给客户端。

### 4.3 周期性维护

1. 每轮 `select` 返回后执行一次挂起请求超时清理。
2. 每轮 `select` 返回后执行一次缓存过期清理。

## 5. 已锁定的实现规则

1. 单线程。
2. 单 UDP socket。
3. 使用 `select`，不使用忙等待。
4. 不对上游超时请求进行重传。
5. 超时时不伪造响应，保持透明性。
6. 只对本地 `A/IN` 记录做本地应答，其他类型透明转发。
7. `QDCOUNT != 1` 或报文格式非法时返回 `FORMERR`。
8. `OPCODE != QUERY` 返回 `NOTIMP`。

## 6. 构建和测试

### Windows

```powershell
cmake -S . -B build -G "MinGW Makefiles"
cmake --build build -j 4
ctest --test-dir build --output-on-failure
```

### Linux

```bash
cmake -S . -B build
cmake --build build -j
ctest --test-dir build --output-on-failure
```

### 当前测试覆盖

1. `test_local_table`：本地表加载、大小写无关、命中/拦截/未命中。
2. `test_dns_packet`：查询解析、A 响应构造、错误响应构造、TTL 修补。
3. `test_pending_map`：插入、删除、超时回收。
