# 计算机网络课程设计总体计划

> 说明：成员 A 为安宇辰（2024211129）；成员 B 为许久新（2024211130）。

## 1. 项目目标

在第14周结束前完成一个可验收的 `DNS Relay Server`，满足以下验收目标：

1. 使用 C 语言实现 DNS 中继服务器。
2. 支持读取 `dnsrelay.txt` 本地映射表。
3. 本地普通记录直接应答。
4. 本地 `0.0.0.0` 记录返回 `NXDOMAIN`，不直接返回 `0.0.0.0`。
5. 本地未命中时向上游 DNS 转发，并把响应返回客户端。
6. 支持多客户端并发查询。
7. 正确处理请求 ID 转换、上游超时和迟到应答。
8. 具备基础 Cache 与 TTL 递减处理。
9. Windows / Linux 共用一套核心源码。

## 2. 当前代码基线

当前仓库已经落地以下工程骨架，可直接继续开发：

```text
/CMakeLists.txt
/include/dnsrelay/
/src/
/tests/
```

当前代码模块与职责如下：

1. `config`：解析 `dnsrelay [-d|-dd] [dns-server-ipaddr] [filename]`。
2. `platform`：封装 Windows/Linux socket 差异。
3. `logger`：封装 `无日志 / -d / -dd` 三档输出。
4. `dns_packet`：解析 DNS 报文、构造本地应答和错误应答、提取并修补 TTL。
5. `local_table`：加载并查找本地表，大小写无关。
6. `pending_map`：管理客户端 ID 与上游 ID 的映射关系。
7. `cache`：缓存上游响应并按剩余 TTL 返回。
8. `server`：单线程、单 UDP socket、`select` 事件循环。

## 3. 需要掌握的预备知识

1. DNS 协议基础：Header、Question、Answer、Authority、Additional、RCODE、TTL。
2. RFC 1035 关键规则：QNAME 编码、名称压缩、UDP 512 字节限制、ID 匹配。
3. C 语言 UDP 编程：`socket`、`bind`、`recvfrom`、`sendto`、`select`。
4. Windows/Linux 差异：Winsock 初始化、错误码、头文件和构建方式。
5. 调试工具：`nslookup`、`ipconfig /flushdns`、`ipconfig /displaydns`、Wireshark。
6. 数据结构：哈希表、状态表、LRU/近似 LRU、TTL 更新。

## 4. 可执行里程碑

| 周次 | 目标 | 代码范围 | 文档范围 | 通过标准 |
|---|---|---|---|---|
| 第11周 | 冻结骨架 | 建立 `CMake + include/src/tests`；确定模块接口、主循环、状态表、缓存结构 | 完成《代码骨架设计》《总体计划》《分工说明》初稿 | 工程可配置、接口不再大改 |
| 第12周 | 打通最小链路 | 完成本地表读取、查询解析、本地 A 响应、NXDOMAIN、本地未命中转发 | 报告补充协议研究、模块说明 | `test1` 本地解析成功，`test0` 返回不存在，未知域名可转发 |
| 第13周 | 攻克难点 | 完成 `select` 并发、ID 转换、超时清理、迟到响应丢弃、Cache 返回 | 报告补充异常处理、流程图、测试设计 | 20 个并发查询不串包；重复查询能命中 Cache |
| 第14周 | 冻结功能 | 只修 bug，不加新特性；完成双平台构建验证、日志整理和答辩彩排 | 完成最终报告、测试截图、答辩材料 | Windows/Linux 均能构建；测试清单全部通过 |
| 第15周周末 | 验收 | 不再做核心开发，只做演示和小修复 | 提交报告、封面、代码目录 | 两位成员均能独立讲清核心设计 |

## 5. 每周执行清单

### 第11周

1. 阅读任务书、RFC 1035 摘要、Socket 讲义。
2. 锁定工程目录、模块接口和数据结构。
3. 确定单线程 + 单 socket + `select` 方案。
4. 写出分工文档和个人计划。

### 第12周

1. 完成 `local_table.c`。
2. 完成 `dns_packet.c` 的 Query 解析、本地 A 响应、NXDOMAIN。
3. 完成 `server.c` 的最小收包、分流、转发链路。
4. 使用 `nslookup test1 127.0.0.1`、`nslookup test0 127.0.0.1`、`nslookup baidu.com 127.0.0.1` 验证。

### 第13周

1. 完成 `pending_map.c` 的 ID 分配、删除和超时回收。
2. 完成 `cache.c` 的缓存写入、命中和 TTL 修补。
3. 加入迟到应答丢弃逻辑。
4. 做并发测试和上游超时测试。

### 第14周

1. 使用 Windows 完成一次完整演示。
2. 使用 Linux 完成一次构建和逻辑验证。
3. 记录测试截图、日志片段和异常案例。
4. 完成报告定稿与双人答辩彩排。

## 6. 构建与测试命令

### Windows / MinGW

```powershell
cmake -S . -B build -G "MinGW Makefiles"
cmake --build build -j 4
ctest --test-dir build --output-on-failure
```

### 运行示例

```powershell
.\build\dnsrelay.exe -dd 8.8.8.8 dnsrelay.txt
```

### Linux 开发阶段示例

开发阶段若无法绑定 53 端口，可使用：

```bash
cmake -S . -B build -DDNSRELAY_DEV_PORT=8053
cmake --build build -j
ctest --test-dir build --output-on-failure
```

## 7. 第14周前必须通过的测试

1. `test_local_table`、`test_dns_packet`、`test_pending_map`、`test_cache` 全部通过。
2. 本地普通域名解析：`test1 -> 11.111.11.111`。
3. 本地屏蔽域名解析：`test0 -> NXDOMAIN`。
4. 未命中域名可正常中继到上游。
5. 重复查询同一外部域名时，第二次命中 Cache。
6. 多客户端并发查询时不会出现 ID 错乱。
7. 上游 DNS 无响应时，程序不会卡死，仍可继续处理新请求。

当前验证结果：以上 4 个单元测试已全部通过；运行验收项按第14周演示命令继续记录日志和截图。

## 8. 最终提交物

1. 源代码。
2. 构建与运行说明。
3. 课程设计报告。
4. 测试记录与截图。
5. 纸版课程设计报告封面。
