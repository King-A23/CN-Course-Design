# 课程设计分工说明

> 说明：成员 A 为安宇辰（2024211129）；成员 B 为许久新（2024211130）。

## 1. 分工原则

1. 采用“主责模块 + 交叉评审 + 共同答辩”的分工方式。
2. 每位成员都要能解释整体架构，不允许只会自己负责的局部代码。
3. 每周至少进行一次代码同步和一次口头讲解。
4. 报告按章节主责撰写，但提交前统一互审。

## 2. 安宇辰（2024211129）主责

### 2.1 代码主责

安宇辰（2024211129）负责“协议解析 + 本地应答 + 文档设计”主线，主责文件如下：

1. `include/dnsrelay/dns_packet.h`
2. `src/dns_packet.c`
3. `include/dnsrelay/local_table.h`
4. `src/local_table.c`
5. `tests/test_dns_packet.c`
6. `tests/test_local_table.c`

### 2.2 具体任务

1. 负责 DNS Header、Question 的解析逻辑。
2. 负责本地 A 响应和 `NXDOMAIN` 响应构造。
3. 负责域名大小写归一化与本地表查找规则。
4. 负责 TTL 提取/修补接口与协议说明。
5. 负责协议研究、模块划分、流程图和总体设计章节。

## 3. 许久新（2024211130）主责

### 3.1 代码主责

许久新（2024211130）负责“事件循环 + 转发控制 + 状态管理 + 缓存测试”主线，主责文件如下：

1. `include/dnsrelay/config.h`
2. `include/dnsrelay/platform.h`
3. `include/dnsrelay/logger.h`
4. `include/dnsrelay/pending_map.h`
5. `include/dnsrelay/cache.h`
6. `include/dnsrelay/server.h`
7. `src/config.c`
8. `src/platform_win.c`
9. `src/platform_posix.c`
10. `src/logger.c`
11. `src/pending_map.c`
12. `src/cache.c`
13. `src/server.c`
14. `src/main.c`
15. `tests/test_pending_map.c`
16. `tests/test_cache.c`

### 3.2 具体任务

1. 负责命令行参数解析和双平台构建链路。
2. 负责单 socket + `select` 主循环。
3. 负责客户端 ID 与上游 ID 的转换表。
4. 负责上游转发、超时回收和迟到应答丢弃。
5. 负责 Cache 命中路径和 TTL 递减返回。
6. 负责测试章节、运行结果记录和验收脚本。

## 4. 共同负责事项

1. 共同确定 `单线程 + 单 socket + select` 架构。
2. 共同做第13周联调，避免接口理解不一致。
3. 共同完成 Windows/Linux 双平台运行验证。
4. 共同完成课程设计报告总审与答辩彩排。
5. 共同准备以下现场演示：
   - 本地解析
   - 域名拦截
   - 上游中继
   - Cache 命中
   - 并发查询

## 5. 协作规则

1. 涉及公共头文件的修改，先同步再合并。
2. 安宇辰（2024211129）优先定义解析/本地表接口，许久新（2024211130）围绕接口集成主循环。
3. 许久新（2024211130）若需要额外字段，由安宇辰（2024211129）先确认结构体是否扩展。
4. 测试用例必须和主代码一起提交，不能后补。

## 6. 答辩分工

| 验收点 | 安宇辰（2024211129）重点说明 | 许久新（2024211130）重点说明 |
|---|---|---|
| DNS 报文结构 | Header、Question、RCODE、TTL、名称压缩 | 转发后的透明性和字段保持 |
| 本地表命中 | 大小写无关、重复项覆盖、`0.0.0.0` 拦截 | 如何与事件循环衔接 |
| 并发处理 | 查询解析和模块接口 | `select`、ID 转换、超时回收 |
| 缓存 | TTL 修补原理 | 命中路径、生命周期和淘汰 |
| 报告与测试 | 协议研究、设计图 | 测试方法、异常场景、演示步骤 |
